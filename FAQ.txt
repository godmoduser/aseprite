для добавления функций необходимо создать 2 файла.

1)send_mesg без расширения:
выполняется единожды, перед лонгполл_листен.
можно использовать для отправки сообщения в формате:

send_msg(3, "как дела") -- отправляет сообщение в клуб успешных

не проверено использование в несколько строк, лучше оформлять всё в одну



2)answer_check без расширения:
выполняется   в лонгполл листене, можно написать обработчик слов в формате:

if mesg.lower().rstrip().endswith(" да") or mesg.lower()=="да" or mesg.lower().rstrip().endswith(" да.") or mesg.lower()=="да.":
                            api.messages.send(
                                        chat_id=usid,
                                        message="Пизда!",
                                        random_id=random.randint(1,999999999),
                                        
                                        )
                            continue

не проверено использование в несколько строк, лучше оформлять всё в одну

изменять эти файлы лучше всего в блокноте.